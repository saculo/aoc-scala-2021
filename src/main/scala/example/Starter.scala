package example

object Starter extends App {
        println(FirstDay.measurements())
        println(FirstDay.measurementsWindows())
        println(SecondDay.dummyPosition())
        println(SecondDay.position())
}
